{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {},
    "lint": {},
    "test": {},
    "ci": {
      "dependsOn": ["build", "lint", "test"],
      "env": ["BLOCKFROST_API_KEY", "SEED_PHRASE"]
    }
  }
}
